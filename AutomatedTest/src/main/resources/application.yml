security:
  basic-auth:
    password: ${BIOMETRIC_AUTH_PASS}
    username: ${BIOMETRIC_AUTH_USER}
test:
  host: ${ORDS_HOST}
  password: ${BIOMETRIC_AUTH_PASS}
  username: ${BIOMETRIC_AUTH_USER}
  api-host-identity: ${API_HOST_IDENTITY}
  api-host-biometrics: ${API_HOST_BIOMETRICS}

